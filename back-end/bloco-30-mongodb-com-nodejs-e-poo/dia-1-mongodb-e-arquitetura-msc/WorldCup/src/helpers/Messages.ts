enum errorMessages {
  internalError = 'Internal server error',
  notFoundError = 'Not found any world cup with this field',
  validationError = 'tournaments validation failed',
}

export default errorMessages;